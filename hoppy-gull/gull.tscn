[gd_scene load_steps=4 format=3 uid="uid://ce6on0dc1jgp1"]

[sub_resource type="GDScript" id="GDScript_wbb4c"]
script/source = "extends CharacterBody3D


const SPEED = 4

@onready var current_position : int = 3
var final_position : Vector3
var movement_dist : float
var movement_time: float

@onready var state_machine: StateMachine


signal check_position

func _ready() -> void:
	final_position = position
	movement_dist = $CollisionShape3D.shape.radius * 2
	state_machine = $\"../state_machine\"
	movement_time = 0.0
	
func _physics_process(delta: float) -> void:

	if (final_position-position).length() >= 1e-2:
		movement_time += delta
		move_and_slide()
	else:
		velocity = Vector3.ZERO
	if velocity == Vector3.ZERO and state_machine.current_state.name == 'moving':
		print(movement_time)
		movement_time = 0.0
		state_machine.set_state(\"alive\")
	pass
		
func _input(event: InputEvent) -> void:
	if event.is_action(\"ui_left\") and velocity.is_zero_approx() \\
	and state_machine.current_state.name == \"alive\"\\
	and current_position > 0:
		final_position = (position + Vector3(movement_dist,0,0)).snappedf(0.1)
		var direction := (transform.basis * Vector3(1,0,0)).normalized()
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
		current_position -= 1
		check_position.emit(final_position, false)
	elif event.is_action(\"ui_right\") and velocity.is_zero_approx()\\
	and state_machine.current_state.name == \"alive\"\\
	and current_position < 6:
		final_position = (position + Vector3(-movement_dist,0,0)).snappedf(0.1)
		var direction := (transform.basis * Vector3(-1,0,0)).normalized()
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
		current_position += 1
		check_position.emit(final_position, false)
	elif (event.is_action_pressed(\"ui_up\") or event.is_action(\"ui_up\"))\\
	 and velocity.is_zero_approx()\\
	and state_machine.current_state.name == \"alive\":
		final_position = (position + Vector3(0,0,movement_dist)).snappedf(0.1)
		var direction := (transform.basis * Vector3(0,0,1)).normalized()
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
		check_position.emit(final_position, true)
		
"

[sub_resource type="CylinderShape3D" id="CylinderShape3D_wbb4c"]
height = 0.5
radius = 0.2

[sub_resource type="CylinderMesh" id="CylinderMesh_2sgct"]
top_radius = 0.2
bottom_radius = 0.2
height = 0.5

[node name="gull" type="CharacterBody3D"]
script = SubResource("GDScript_wbb4c")

[node name="CollisionShape3D" type="CollisionShape3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.25, 0)
shape = SubResource("CylinderShape3D_wbb4c")

[node name="MeshInstance3D" type="MeshInstance3D" parent="CollisionShape3D"]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, -0.00012497604, 0)
mesh = SubResource("CylinderMesh_2sgct")
skeleton = NodePath("../..")
